// Simple test file for workflow validation
describe('Sample tests', () => {
  test('basic arithmetic', () => {
    expect(1 + 1).toBe(2);
  });
  
  test('string operations', () => {
    expect('hello'.toUpperCase()).toBe('HELLO');
  });
});