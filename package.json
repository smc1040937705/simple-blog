{
  "name": "simple-blog",
  "version": "1.0.0",
  "description": "A simple blog application",
  "main": "src/index.html",
  "scripts": {
    "lint": "eslint src/js/*.js",
    "format:check": "prettier --check src/**/*.{js,html,css}",
    "format:write": "prettier --write src/**/*.{js,html,css}",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "build": "mkdir -p dist && cp -r src/* dist/"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^2.0.0",
    "jest": "^29.0.0"
  },
  "keywords": ["blog", "simple"],
  "author": "",
  "license": "MIT"
}